Convert this current HTML/CSS/JavaScript frontend into a 1-to-1 Next.js App Router project with React and TypeScript. Keep the UI, styling, class names, layout, text, and behavior exactly the same. Do not redesign anything. Port the CSS into global styles, convert JavaScript to TypeScript, replace old page navigation with Next.js routing, and keep the current hardcoded/mock content for now. The result should be a direct page-by-page copy of the existing frontend in Next.js.